# Configure these names in Streamlit Community Cloud under App settings > Secrets.
# Never commit real credentials.
REUTERS_CLIENT_ID = "your-client-id"
REUTERS_CLIENT_SECRET = "your-client-secret"
REUTERS_AUDIENCE = "your-audience"

# The confirmed token and GraphQL endpoints are built into the app.
